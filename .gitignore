node_modules/
dist/
.astro/
.env
.env.*
!.env.example
.playwright-mcp/
*.png
!public/og-image.png
!public/docs/**/*.png

# Source HTML for regenerating public/og-image.png via headless screenshot
og-image.source.html

# Windows Explorer metadata
desktop.ini

# Claude Code local settings (personal, not shared)
.claude/settings.local.json

# Superpowers dev-process artifacts (specs, plans) — scratch, not part of the project
docs/superpowers/

# Per-project bulk artifacts — test corpora stay local-only.
# README.md, plans/*.md and eval/ (small Python harness) are tracked.
projects/**/test_data/

# Python build artifacts (eval runners or any future Python may produce these)
__pycache__/
*.pyc
